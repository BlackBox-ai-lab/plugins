
# Scratchpad archive holds tailnet URLs — never commit it to this public repo.
scratch_archive/
# Machine-specific leak patterns for release-preflight (yours, not the world's).
scripts/.leak-patterns.local

scripts/.leak-allow.local
